import {Configuration} from 'electron-builder'

export default function (): Configuration {
    return {
        /**
         * @see https://www.electron.build/configuration/configuration
         * Custom build options that will override the default settings.
         */
    }
}
